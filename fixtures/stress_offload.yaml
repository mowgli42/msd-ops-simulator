# Stress scenario — offload-bound high tempo (for briefing contrast)
scenario_id: stress_offload

timing:
  ticks_per_hour: 20
  operating_hours_per_day: 24

operations:
  vehicles: 12
  missions_per_vehicle_per_day: 6.0
  ports_per_vehicle: 2
  devices_per_mission: 1
  min_devices_per_vehicle: 1
  preload_all_ports: false
  device_pool: 24
  loading_stations: 2
  offload_stations: 2

durations_hours:
  mission: 2.0
  load: 0.5
  offload: 1.0
  install: 0.0
  sanitize: 0.0

modes:
  high_data_volume: false
  offload_factor: 0.9

analysis:
  utilization_target: 0.85
  device_buffer_fraction: 0.10
